import React from "react";

const Header = ({}) => {
  return (
    <header className="h-40 w-full p-4 flex justify-between align-center border-2">
      <h1 className="text-xl font-bold">SGA PORTAL</h1>
    </header>
  );
};

export default Header;
